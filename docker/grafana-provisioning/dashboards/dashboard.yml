apiVersion: 1
providers:
- name: InfluxDB
  folder: ''
  type: file
  access: "proxy"
  user: "$INFLUX_USER"
  password: "$INFLUX_PASSWORD"
  database: "$INFLUX_DB"
  basicAuth: false
  disableDeletion: false
  editable: true
  options:
    path: /etc/grafana/provisioning/dashboards